spring:
  datasource:
    url: jdbc:h2:mem:digital_treehole;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true
  servlet:
    multipart:
      location: ./target/test-upload-tmp

treehole:
  auth:
    jwt-secret: test-secret-test-secret-test-secret-test-secret
  storage:
    doodle-dir: ./target/test-doodles
    public-base-url: http://localhost:8080
